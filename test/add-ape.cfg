process TEST = {

    // Message logger service
    include "FWCore/MessageService/data/MessageLogger.cfi"

    // Ideal geometry producer
    include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
    include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"
    include "Geometry/TrackerGeometryBuilder/data/trackerGeometry.cfi"

    // Database output service
    include "CondCore/DBCommon/data/CondDBCommon.cfi"
    replace CondDBCommon.connect = "sqlite_file:TrackerIdealGeometry.db"    
    replace CondDBCommon.catalog = "file:TrackerIdealGeometry.xml"
    service = PoolDBOutputService {
        using CondDBCommon
        VPSet toPut = {
            { string record = "TrackerAlignmentErrorRcd" string tag = "TrackerNoErrors150" }
        }
    }

    // Empty event source
    source = EmptySource { }
    untracked PSet maxEvents = { untracked int32 input = 1 }

    // Test module
    module addApe =  ApeAdder { untracked vdouble apeVector = { 0., 0., 0. } }

    path p1 = { addApe }      
    
    // Print some stats at the end
    module print = AsciiOutputModule {}
    endpath ep = { print }

}
