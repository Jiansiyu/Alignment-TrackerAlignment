process TEST = {
        
    // Message logger service
    include "FWCore/MessageService/data/MessageLogger.cfi"
#    replace MessageLogger.cout.default.limit = 100000

    // Setup tracker
    include "Alignment/TrackerAlignment/data/TrackerSetup.cff"

    // Empty event source
    untracked PSet maxEvents = { untracked int32 input = 1 }
    source = EmptySource { }
    
    // Test module
    module prod =  TestTrackerHierarchy { }
    
    path p1 = { TrackerSetup, prod }      
    
}
